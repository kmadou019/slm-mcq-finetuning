# FastAPI and web server (minimal for testing)
fastapi>=0.100.0
uvicorn>=0.23.0

# Optional: Add these for production
python-jose[cryptography]  # For JWT
passlib[bcrypt]            # For password hashing
python-dotenv              # For environment variables
